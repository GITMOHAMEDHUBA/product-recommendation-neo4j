version: "3.8"

services:
  neo4j:
    image: neo4j:5
    container_name: neo4j_rec
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=neo4j/password
      - NEO4J_PLUGINS=["apoc"]
    volumes:
      - ../neo4j:/var/lib/neo4j/import

